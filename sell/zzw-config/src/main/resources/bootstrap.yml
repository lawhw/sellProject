server:
  port: 8888
spring:
  rabbitmq:
    host: zzw-rabbitmq
    port: 5672
    username: zhangzhiwen
    password: 113536
  application:
    name: zzw-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/
eureka:
  client:
    service-url:
      defaultZone: http://zhang:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: '*'